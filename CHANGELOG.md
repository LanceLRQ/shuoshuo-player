## Changelog

本文件记录说说播放器 v2 工作区的发布历史。版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/)。

> 1.x 区间为 **Beta**；2.0+ 起进入稳定版。

### [Unreleased]

#### 新增

- **UP 主歌单页多了「合集」标签页**：能看到这位 UP 主在 B 站整理的视频合集，点进去就是合集里的全部视频，可以一键回到合集列表。
- **合集一键播放与加入歌单**：合集详情里可以把整个合集当作播放列表播放，或者把合集里的每首歌挑进自己的自定义歌单。
- **加载合集进度框**：加载合集时会弹出进度对话框，能看到当前已加载多少首；可以随时取消，已加载的部分会保留，方便接着选择「加入歌单」。
- **UP 主投稿和收藏夹也加进度框**：现在加载这两类列表时也会弹出同款进度框，可以看到进度并随时取消。
- **新增「检查更新」**：UP 主歌单 / 收藏夹的菜单里替换原来的「更新前 30」，改为智能增量模式：只拉新增的视频，碰到本地已存的就停下，通常 1 次请求就够；本地没缓存时自动拉前 90 条做兜底，不再上来就全量。
- **新增「按范围拉取」**：UP 主歌单 / 收藏夹 / 合集都加了这个入口，让你自己选第几页到第几页加载，对大投稿 UP 友好。
- **新增「重新拉取全部」**：完整重拉后会把本地有但远端没有（被作者删除）的视频在歌单里标记为「已失效」，自定义歌单引用这些视频时也会显示该标记，禁止播放但保留记录方便手动整理。
- **新建歌单更顺手**：「添加到歌单」弹窗顶部多了一行「+ 新建歌单」，直接输入名字就能就地创建并自动勾选，不用再退出来去歌单页单独建。

#### 变更

- 设置页新增「播放」标签页，把原先放在「外观」里的"多 P 投稿连续播放"开关搬到这里集中管理，同时新增"播放合集时是替换当前队列还是接在后面"的选项。
- 合集如果很大（超过 150 首，常见于直播切片号），播放按钮会变成下拉菜单，可以选「第 1-5 页（最新约 150 首）」或更小范围分批加载，避免一次性请求过多被 B 站风控；菜单底部还有「自定义范围…」让你按页精确选取。
- 合集详情的「加入歌单」按钮改为分体式：直接点按钮主体就把当前已加载的视频加入歌单（不发请求）；点右侧 ▾ 展开下拉菜单可以选某个分页范围、自定义范围或一次性全部，与播放按钮对称。
- 首页主歌单自动 24h 刷新改为走「检查更新」增量模式，不再首次进入就把整个投稿历史全拉，明显降低风控概率。

#### 修复

- 合集全量拉取改为串行模式（页之间间隔 200 毫秒），大幅降低被 B 站风控的概率。
- UP 主投稿和收藏夹的拉取也改为统一串行节奏（300 毫秒页间隔），同步缓解风控压力。

### [1.9.3] - 2026-05-13

#### 新增

- **v1 旧版数据自动迁移**：从 v1 旧版升级到新版后，会自动检测浏览器里残留的旧数据并弹层引导你导入，可选「立即导入」、「稍后再说」或「永久放弃」。永久放弃前还能下载一份旧数据 JSON 备份保底，避免误操作丢失歌单。

#### 变更

- 默认粉色加深一档，色调更耐看。
- 主色按钮文字色按主色亮度智能适配：浅色主色（粉/黄/浅绿/天蓝等）用同色系深字，深色主色（深红/深蓝/深紫等）用白字，再不会出现「黄底白字看不见」的尴尬。
- 暗色主题下使用主题色作为文字（如「退出登录」红字、自定义深色主色文字）会自动调亮，深底上更清晰。
- focus 高光颜色跟随用户自定义主色，不再固定粉色。
- 播放器进度条已播放和未播放部分对比更分明，能直观看到进度；圆形滑块改为跟随主题色填充；平时收起来不打扰，鼠标移到进度条上才出现，离开 1 秒后自动隐藏。
- 顶部栏右半区相对左半区下移 8px 形成层次感，内部文字保持与左侧 logo 水平对齐。
- 数据导入预览弹窗加宽，并新增「→」箭头表达迁移方向。

#### 修复

- 加固多项安全防护：导入数据更严格的合法性校验、外链协议白名单校验、API 请求限速，避免 JSON 内容引发的 XSS 注入风险。

### [1.9.2] - 2026-05-13

#### 新增

- **悬浮歌词独立开关**：右下角新增专门的开关按钮，可以单独关闭，状态会记住。
- **悬浮歌词自定义样式**：「设置 → 外观」可调字号、字重、字体、对齐、高度、文字颜色和透明度。
- 悬浮歌词样式调整时支持实时预览。
- **B 站多 P 视频支持选 P 播放**：可挑选具体某一个分 P，会记住上次的选择。
- 分 P 选择面板支持一次勾选多个分 P 加入歌单。
- **「我的收藏」系统歌单**：左下角新增一键收藏按钮，喜欢的歌随手加入。
- 左侧栏布局重新整理。
- **视频列表点击更顺手**：双击列表项或点封面直接播放。

#### 变更

- 进入全屏歌词的入口统一收到左下角播放器封面，悬停时显示展开提示。
- 视频列表右侧按钮从播放改为收藏，避免误触。

#### 修复

- 修复快速切歌时偶尔出现两首歌同时响起的问题。
- 修复收藏夹歌单页的若干显示问题。
- 优化歌单卡片的使用体验。

### [1.9.1] - 2026-05-11

#### 新增

- **自动检查更新**：启动时检测新版本，有更新会在顶栏弹出通知提醒你升级。
- **设置页「关于」**：可以查看当前版本号、更新通道和项目信息，Chrome 扩展还有直达商店的入口。
- **网络故障自动重试**：播放链接获取失败时会自动重试，不再直接报错。
- **Windows 免安装版**：下载即可双击运行，无需安装，数据跟程序放一起，U 盘也能带走。

#### 变更

- 安装指南新增国内镜像入口，下载更方便。

#### 修复

- 修复 Windows 桌面端部分音频无法播放的问题。
- 修复播放栏滚动、弹窗溢出、Banner 切换等若干界面问题。

### [1.9.0] - 2026-05-09

> v2 重构后首个 Beta 发布版本（标签 `v1.9.0-rc.1`），1.x 区间整体为 Beta 通道。

#### 新增

- **「水晶蟹小屋」云服务模块**：全新视觉与交互，登录入口直接在首页，体验更顺滑。
- **B 站退出登录**：恢复了退出登录功能，退出后不会再残留登录状态。
- **数据导入导出升级**：导入时支持智能合并策略，可以保留已有歌单数据不丢失；兼容旧版数据导入。
- **搜索发现页大幅扩展**：支持批量勾选搜索结果加入歌单；结果上限提升至 1000 条，支持分页浏览和排序。
- **歌单卡片封面优化**：没有视频的歌单也能显示漂亮的默认封面了。
- **收藏夹歌单创建优化**：可以直接从下拉列表选择 B 站收藏夹，自动填好名称。
- **Chrome 扩展支持版权曲播放**：新增合作音乐源白名单，更多歌曲可正常播放。
- **Beta 徽章**：顶栏和导入对话框标明当前为 Beta 版本。
- **全新图标**：所有平台图标焕新，macOS Dock 显示中文名「说说播放器」。

#### 变更

- 导入对话框界面更紧凑，合并策略更简洁。
- 导出功能在所有平台统一了行为，更稳定可靠。
- 切歌更快了，冷启动延迟减少约 1-2 秒。

#### 修复

- 修复桌面端部分老视频无法播放的问题，现在会自动尝试备用音质。
- 修复第三方音乐源在桌面端无法使用的问题。
- 修复导入自定义歌单后内容显示为空的问题。
- 修复部分类型歌单顶部的播放按钮无法点击的问题。
- 修复重复点击导致歌单重复加载的问题。
- 修复播放队列列表项截断、清空按钮重叠等若干视觉问题。

### 历史版本

更早版本（v1.8.x 及之前）仅在 GitHub Releases 中记录，未维护本文件。
